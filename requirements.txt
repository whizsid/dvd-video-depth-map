# Deps for DepthCrafter video depth (CUDA).
# CUDA-only packages (xformers) are optional — enabled at runtime if present.
# Colab provides CUDA torch; local installs need a CUDA-capable torch build.

torch>=2.5.0
torchvision>=0.20.0
transformers>=4.40.0
diffusers>=0.30.0
accelerate>=0.33.0
safetensors
einops
sentencepiece
protobuf
huggingface_hub>=0.23.0
opencv-python-headless>=4.8.0
imageio
imageio[ffmpeg]
numpy
Pillow
tqdm
psutil>=5.9.0
# Optional video decode accel used by upstream DepthCrafter utils (we use OpenCV):
# decord
# Optional: xformers (CUDA) for memory-efficient attention
