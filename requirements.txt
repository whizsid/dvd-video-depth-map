# Shared deps for DepthCrafter video depth (CUDA) and DA3 photo depth.
# CUDA-only packages (xformers) are optional — enabled at runtime if present.
# Colab provides CUDA torch; Mac venv is primarily for photo/DA3.

torch>=2.5.0
torchvision>=0.20.0
transformers>=4.40.0
diffusers>=0.30.0
accelerate>=0.33.0
safetensors
einops
sentencepiece
protobuf
huggingface_hub>=0.23.0
opencv-python-headless>=4.8.0
imageio
imageio[ffmpeg]
numpy
Pillow
tqdm
psutil>=5.9.0
rawpy>=0.22.0
# Photo depth (DA3): pip install git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
# Optional video decode accel used by upstream DepthCrafter utils (we use OpenCV):
# decord
# Optional: xformers (CUDA) for memory-efficient attention
